_schema-version: "3.2"
ID: zerotouch
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: zerotouch-destination-content
  type: com.sap.application.content
  requires:
  - name: zerotouch-destination-service
    parameters:
      content-target: true
  - name: zerotouch-repo-host
    parameters:
      service-key:
        name: zerotouch-repo-host-key
  - name: zerotouch-uaa
    parameters:
      service-key:
        name: zerotouch-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: zerotouch_html_repo_host
          ServiceInstanceName: zerotouch-html5-srv
          ServiceKeyName: zerotouch-repo-host-key
          sap.cloud.service: zerotouch
        - Authentication: OAuth2UserTokenExchange
          Name: zerotouch_uaa
          ServiceInstanceName: zerotouch-xsuaa-srv
          ServiceKeyName: zerotouch-uaa-key
          sap.cloud.service: zerotouch
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: zerotouch-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: zerotouch-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - zerotouch.zip
      name: zerotouch
      target-path: resources/
- name: zerotouch
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: zerotouch-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: zerotouch-destination-service
    service-plan: lite
- name: zerotouch-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: zerotouch-xsuaa-srv
    service-plan: application
- name: zerotouch-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: zerotouch-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
